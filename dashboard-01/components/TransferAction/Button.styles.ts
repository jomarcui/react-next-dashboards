import styled from '@emotion/styled';

export const ButtonStyles = styled.button`
  background-color: #fff;
  border: none;
  border-radius: 2em;
  margin: 1em;
  min-width: 10.188rem;
  padding: 2em;

  &:first-of-type {
    margin-left: 0;
  }

  &:last-of-type {
    margin-right: 0;
  }
`;
